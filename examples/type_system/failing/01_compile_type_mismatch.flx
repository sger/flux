// parity-expected-stdout-begin
// parity-expected-stdout-end
// expect: compile_error

fn add(a: Int, b: Int) -> Int {
    a + b
}

// Compile-time mismatch: argument #1 is String, expected Int.
add("x", 2)
