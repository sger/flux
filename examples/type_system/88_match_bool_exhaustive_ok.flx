// parity-expected-stdout-begin
// parity-expected-stdout-end

// General match exhaustiveness: Bool domain is exhaustive without catch-all.
fn main() {
    let b = true
    let out = match b {
        true -> 1,
        false -> 0
    }
    out
}
