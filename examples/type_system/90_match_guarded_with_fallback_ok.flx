// parity-expected-stdout-begin
// parity-expected-stdout-end

// Guarded arm does not close coverage; unguarded fallback does.
fn main() {
    let n = 3
    let out = match n {
        x if x > 10 -> 1,
        _ -> 0
    }
    out
}
