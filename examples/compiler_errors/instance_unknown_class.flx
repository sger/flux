// expect: compile_error
// expect-error: E441
// KNOWN GAP: this fixture declares `expect: compile_error`, but E440/E441/E442
// are still routed as warnings, so the compiler accepts this file and exits 0.
// See the partition in src/compiler/mod.rs (collect_class_declarations).

// Instance declaration for a class that doesn't exist.
//
// Expected: E441 Unknown Type Class
//   "No type class `Foo` is defined."
//   Hint: "Declare the class first: `class Foo<a> { ... }`"

instance Foo<Int> {
    fn bar(x) { x }
}

fn main() {
    42
}
