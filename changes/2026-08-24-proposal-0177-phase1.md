### Docs
- Recorded that proposal 0177 Phases 0 and 1 have shipped: the proposal and the
  index now carry per-phase status, a summary of what landed, and answers to the
  Phase 0/1 unresolved questions that the implementation settled.
- KI-020: `flux test` only runs the entry file's tests, so tests in other
  modules of a package are silently never executed.
