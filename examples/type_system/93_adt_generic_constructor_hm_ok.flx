// parity-expected-stdout-begin
// parity-expected-stdout-end

type Result<T, E> = Ok(T) | Err(E)

fn unwrap_or_inc(r: Result<Int, String>) -> Int {
    match r {
        Ok(v) -> v + 1,
        Err(_) -> 0,
    }
}

let x: Result<Int, String> = Ok(1)
let y: Int = unwrap_or_inc(x)
