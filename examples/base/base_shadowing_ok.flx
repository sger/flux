// Phase 3 Base qualification under shadowing
// Bare `len` resolves to local binding; `Base.len` resolves to Base.

fn demo() {
  let len = fn(x) { 999 };
  let xs = [1, 2, 3];

  print(len(xs));
  print(Base.len(xs));
}

demo();
