// parity-expected-stdout-begin
// parity-expected-stdout-end
// root: examples/type_system

module TypeSystem.StrictPrivateHelpers {
    fn helper(x) {
        x + 1
    }

    public fn run(x: Int) -> Int {
        helper(x)
    }
}
