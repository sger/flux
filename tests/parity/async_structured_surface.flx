// parity: vm, llvm
// expect: success
// Proposal 0174 Phase 1b: source-visible structured concurrency surface.

import Flow.Async as A

fn main() with IO, Async {
    let value = A.run_async(fn() { 5 });
    print(value);
}
