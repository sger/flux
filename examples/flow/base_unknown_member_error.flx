// parity-expected-stdout-begin
// parity-expected-stdout-end

// expect: compile_error
// Expected compile error E080: unknown Flow member

import Flow

fn main() with IO {
    print(Flow.not_real())
}