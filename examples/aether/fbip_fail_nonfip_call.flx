// parity-expected-stdout-begin
// parity-expected-stdout-end

// Aether FBIP failure regression fixture.

fn alloc(x) {
    Some(x)
}

@fbip fn bounded(f, x) {
    f(x)
}

fn main() {
    bounded(alloc, 1)
}