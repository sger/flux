// expect: compile_error
// expect-error: E472
// This baseline fixture verifies that a type constructor must be fully applied
// when a concrete type is required.
data Box<a> { Box(a) }
fn take(value: Box) -> Int { 1 }
fn main() { 1 }
