- Feature Name: Compiler Phase Pipeline Refactor and Performance Roadmap
- Start Date: 2026-02-26
- Status: Partially Implemented
- Proposal PR: 
- Flux Issue: 

# Proposal 0044: Compiler Phase Pipeline Refactor and Performance Roadmap

## Summary
[summary]: #summary

Refactor Flux compiler architecture into explicit, testable phase modules first, then layer performance improvements on top.  
Current compiler complexity is concentrated in:
- `src/bytecode/compiler/mod.rs` (~2178 LOC)
- `src/bytecode/compiler/expression.rs` (~2625 LOC)

## Motivation
[motivation]: #motivation

The proposal addresses correctness, maintainability, and diagnostics consistency for this feature area. It exists to make expected behavior explicit and testable across compiler, runtime, and documentation workflows.

## Guide-level explanation
[guide-level-explanation]: #guide-level-explanation

### 3. Goals

1. Make compiler phases explicit and composable.
2. Preserve existing language semantics and diagnostics.
3. Establish measurable compile-time performance baseline and improvement targets.
4. Enable focused optimization work without destabilizing type/effect correctness.
5. Keep VM/JIT compile-time diagnostics parity intact.

### 4. Non-Goals

1. New language features or syntax changes.
2. Replacing HM inference/effect-row algorithms in this proposal.
3. Runtime/JIT execution optimization of generated bytecode.
4. Rewriting `expression.rs` fully in one pass.

### 3. Goals

### 4. Non-Goals

### 4. Non-Goals

## Reference-level explanation
[reference-level-explanation]: #reference-level-explanation

The technical details below consolidate implementation, validation, and policy notes from the legacy proposal.

### Consolidated technical points

- **Consolidated technical points:** - **__preamble__:** Implementation note: - Phase-runner starter scaffolding has been introduced (`pipeline.rs`, `passes/prepare.rs`) with behavior-preserving delegation to the e...
- **Detailed specification (migrated legacy content):** Implementation note: - Phase-runner starter scaffolding has been introduced (`pipeline.rs`, `passes/prepare.rs`) with behavior-preserving delegation to the existing compile path...
- **Current pain points:** 1. **Monolithic orchestration:** `compile()` mixes state reset, inference, validations, and codegen. 2. **Implicit phase contracts:** pass order is encoded only by call order, h...
- **Why architecture-first:** A clean pass pipeline is the prerequisite for safe and measurable performance optimization in a compiler with strict diagnostic contracts (A–G from 043).
- **5.1 Phase Pipeline Model:** Introduce a pass runner with explicit ordered phases: 1. `Prepare` 2. `CollectContractsAndDecls` 3. `InferUnannotatedEffects` 4. `ValidateEntrypointAndPurity` 5. `PredeclareFunc...
- **5.2 New Internal Modules:** Create: - `src/bytecode/compiler/pipeline.rs` - `src/bytecode/compiler/passes/mod.rs` - `src/bytecode/compiler/passes/prepare.rs` - `src/bytecode/compiler/passes/collect.rs` - `...

### Detailed specification (migrated legacy content)

This proposal was already largely template-structured before corpus normalization. Detailed normative text is captured in the sections above.

### Historical notes

- No additional historical metadata was found in the legacy document.

## Drawbacks
[drawbacks]: #drawbacks

### 4. Non-Goals

1. New language features or syntax changes.
2. Replacing HM inference/effect-row algorithms in this proposal.
3. Runtime/JIT execution optimization of generated bytecode.
4. Rewriting `expression.rs` fully in one pass.

### 4. Non-Goals

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

### 12. Risks and Mitigations

1. **Risk:** Subtle behavior drift during extraction  
   **Mitigation:** phase-by-phase moves + snapshot parity gating.

2. **Risk:** Over-refactor delays performance gains  
   **Mitigation:** fixed Milestone 1 scope; no semantic rewrites.

3. **Risk:** Perf regressions from extra abstraction  
   **Mitigation:** baseline-first metrics and per-phase timers.

4. **Risk:** Diagnostic ordering changes  
   **Mitigation:** explicit ordering tests and parity snapshot checks.

### 4. Non-Goals

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

### 12. Risks and Mitigations

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

## Rationale and alternatives
[rationale-and-alternatives]: #rationale-and-alternatives

1. A strict template improves comparability and review quality across proposals.
2. Preserving migrated technical content avoids loss of implementation context.
3. Historical notes keep prior status decisions auditable without duplicating top-level metadata.

## Prior art
[prior-art]: #prior-art

The technical details below consolidate implementation, validation, and policy notes from the legacy proposal.

No additional prior art identified beyond references already listed in the legacy content.

## Unresolved questions
[unresolved-questions]: #unresolved-questions

- No unresolved questions were explicitly listed in the legacy text.
- Follow-up questions should be tracked in Proposal PR and Flux Issue fields when created.

## Future possibilities
[future-possibilities]: #future-possibilities

### Milestone 1 (Architecture split, behavior-preserving)

- introduce pipeline + pass modules
- move logic with minimal edits
- keep all tests green

### Milestone 2 (Measurement harness)

- phase timing and counters in debug/profiling mode
- document baseline numbers

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

### Milestone 4 (Targeted hotpath optimization)

- expression/effect inference memory churn reduction
- symbol-table scope perf improvements

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

### Milestone 1 (Architecture split, behavior-preserving)

### Milestone 2 (Measurement harness)

### Milestone 3 (Low-risk perf wins)

- clone and scan reduction
- artifact reuse

### Milestone 4 (Targeted hotpath optimization)
