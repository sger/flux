// parity-expected-stdout-begin
// parity-expected-stdout-end
// expect: compile_error

// E425: strict mode rejects unresolved generic runtime boundary checks on module-qualified calls.

import TypeSystem.HmGenericModule

fn main() -> Unit {
    TypeSystem.HmGenericModule.id(1)
    None
}
