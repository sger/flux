// E400: Base HOF callback effect requirement must be available in ambient context.

fn noisy_log(n: Int) -> Unit with IO {
    print("tick")
}

fn main() with Time {
    each([1, 2, 3], noisy_log)
}
