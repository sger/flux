// Test Case 5: Suggestions with global variables

let totalCount = 100
let totalValue = 200
let totalAmount = 300

fn main() with IO {
    print(totalCount)
    print(totalValue)
    print(totalAmount)
}