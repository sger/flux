// parity-expected-stdout-begin
// "42"
// parity-expected-stdout-end

// Generic identity keeps the value type precise across the boundary.

fn identity<T>(x: T) -> T {
    x
}

fn main() -> Unit with IO {
    let value = identity(42)
    print(to_string(value))
}
