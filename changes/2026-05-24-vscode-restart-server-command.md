### Added
- VS Code: a **"Flux: Restart Language Server"** command. It restarts the
  `flux-lsp` server process in place, so a freshly rebuilt bundled binary takes
  effect without reloading the whole window (the rust-analyzer / vscode-haskell
  "Restart Language Server" analogue).
