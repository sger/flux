### Added
- Update reported backend name in runtime snapshots
- use stable display paths in module graph diagnostics
- strip Windows verbatim prefixes in display paths
- implement yield-based effect handlers
- Implement fused compare-and-jump for LIR Branch terminator
- Introduce infrastructure for fused compare-and-jump optimization
- Emit direct call opcodes in LIR bytecode emitter
- Implement direct function call opcodes for VM
- Implement merged LIR compilation path (Phase 8b)
- Normalize path display in diagnostics

### Changed
- fix format issues
- remove parity script
- Fix native list reuse ownership and isolate LLVM temp builds
- add example for bug
- fix unit tests
- add parity script
- fix unit test
- fix path issue between windows and unix
- fix windows path issues
- Make DropSpecs count assertion more robust
- Refine Aether report parsing for 'dump-aether' test
- Improve path normalization in snapshot transcript helper
- update snapshot tests
- add tests for stable display paths in diagnostics
- add test for stripping verbatim prefixes in display paths
- add new proposal
- fix parity issues
- remove run lir compiler flag and update arch doc
- fix clippy issues
- fix parity issues with primops lists
- drop LIR VM mode and fix backend stats labels
- Revert "fix(lir): Improve bytecode emission for direct calls and constructors"
- Revert "feat: Implement direct function call opcodes for VM"
- Revert "feat: Emit direct call opcodes in LIR bytecode emitter"
- Revert "feat: Introduce infrastructure for fused compare-and-jump optimization"
- Revert "feat: Implement fused compare-and-jump for LIR Branch terminator"
- Add execution time statistics to main runner
- delete old Core→LLVM codegen
- add Windows support for LLVM native backend pipeline
- fix Aether RC: dup fields extracted from case scrutinees before owned use
- fix Aether RC field-parent aliasing, wire --native to LIR→LLVM, fix FluxClosure alignment
- fix FluxClosure struct alignment for LLVM optimizer correctness
- fix LIR→LLVM: direct calls, constructor dispatch, and Aether RC parity
- Add LirFuncId, extend LirFunction, change MakeClosure, add func_index to LirProgram
- fix higher order functions work on arrays
- fix parity bus between vm and narive llvm
- fix MatchCtor dispatch issues in the LLVM emitter
- fix parity issues from LIR → Bytecode
- Fix cross-block SSA violations from the Aether DropReuse/reuse path
- add LLVM emitter (LIR → LLVM IR)
- fix import aliases bug
- add LIR bytecode emission constructor pattern matching
- add LIR bytecode emission with ADT constructor
- add LIR bytecode emitter with closures, pattern matching, and multi-function support
- add Aether dup/drop/reuse
- add Closures and function calls
- add Pattern matching and ADTs
- add Core → LIR lowering — scalars and let bindings
- add LIR data structures
- update proposal
- fix vm unit tests
- Delete PrimOp and translation layers
- VM dispatch on CorePrimOp → C functions
- Change OpPrimOp to carry CorePrimOp id
- Add `#[repr(u8)]` discriminants to CorePrimOp
- Move data-carrying variants out of CorePrimOp
- Add missing variants to CorePrimOp
- Implement Proposal 0131: replace mark-sweep GC with Aether RC
- add new proposals
- add lists as first class default collection
- Implement Proposal 0125 cons lists as first-class default collection

### Fixed
- Harmonize module path display in runtime error snapshots
- Standardize module path prefix in compiler error snapshots
- normalize snapshot paths for Windows verbatim prefixes
- Improve bytecode emission for direct calls and constructors

