### Added
- `flux-lsp`: `textDocument/implementation` support — invoking "Go to
  Implementations" on a `class` (its declaration name, or any reference to
  it such as the `Show` in `instance Show<Int>`) lists every `instance`
  block implementing that class in the file.
