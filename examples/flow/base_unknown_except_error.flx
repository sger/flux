// parity-expected-stdout-begin
// parity-expected-stdout-end

// expect: compile_error
// Expected compile error E080: unknown Flow member in except list

import Flow except [does_not_exist]

fn main() with IO {
    print(Flow.not_real())
}