type Shape = Circle(Float) | Rect(Float, Float)

fn area(s: Shape) -> Float {
    match s {
        Circle(r) -> r * r,
        Rect(w, h) -> w * h,
    }
}

let unit_circle_area: Float = area(Circle(1.0))
let rect_area: Float = area(Rect(2.0, 3.0))
