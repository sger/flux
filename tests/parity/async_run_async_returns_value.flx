// parity: vm
// expect: success
// bug: run_async must return the value produced by the async body.
//      Sequential-equivalent VM semantics: action is called synchronously.

import Flow.Async exposing (..)

fn body() -> Int with Async { 42 }

fn main() with IO {
    let v = run_async(body)
    print(v)
}
