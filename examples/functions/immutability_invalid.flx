// Error demo: direct assignment is not allowed in Flux.
let a = 5;
a = 10;

// Other invalid assignment shapes (uncomment to test individually):
// c = 5; // undefined variable
// fun increment(x) {
//     x = x + 1; // parameter reassignment is also rejected
//     return x;
// }
