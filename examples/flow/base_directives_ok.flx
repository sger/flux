// Phase 3 Flow directives: valid usage
// - excludes unqualified `print`
// - still allows qualified `Flow.print`

import Flow except [print]

let xs = [10, 20, 30, 40];
Flow.print("len(xs) = " + to_string(len(xs)));
Flow.print("first(xs) = " + to_string(first(xs)));
