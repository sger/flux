---
source: tests/aether_cli_snapshots.rs
---
letrec __tc_Sizeable_Int_size =
λx.
    1

letrec __tc_Sizeable_String_size =
λx.
    Len(x)

letrec __tc_Eq_Int_eq =
λ__x0, __x1.
    ICmpEq(__x0, __x1)

letrec __tc_Eq_Int_neq =
λ__x0, __x1.
    ICmpNe(__x0, __x1)

letrec __tc_Eq_Float_eq =
λ__x0, __x1.
    FCmpEq(__x0, __x1)

letrec __tc_Eq_Float_neq =
λ__x0, __x1.
    FCmpNe(__x0, __x1)

letrec __tc_Eq_String_eq =
λ__x0, __x1.
    Eq(__x0, __x1)

letrec __tc_Eq_String_neq =
λ__x0, __x1.
    NEq(__x0, __x1)

letrec __tc_Eq_Bool_eq =
λ__x0, __x1.
    Eq(__x0, __x1)

letrec __tc_Eq_Bool_neq =
λ__x0, __x1.
    NEq(__x0, __x1)

letrec __tc_Ord_Int_compare =
λ__x0, __x1.
    let %t1 = ICmpLt(__x0, __x1)
    case %t1 of
      true →
        -1
      _ →
        let %t2 = ICmpGt(__x0, __x1)
        case %t2 of
          true →
            1
          _ →
            0

letrec __tc_Ord_Int_lt =
λ__x0, __x1.
    ICmpLt(__x0, __x1)

letrec __tc_Ord_Int_lte =
λ__x0, __x1.
    ICmpLe(__x0, __x1)

letrec __tc_Ord_Int_gt =
λ__x0, __x1.
    ICmpGt(__x0, __x1)

letrec __tc_Ord_Int_gte =
λ__x0, __x1.
    ICmpGe(__x0, __x1)

letrec __tc_Ord_Float_compare =
λ__x0, __x1.
    let %t3 = FCmpLt(__x0, __x1)
    case %t3 of
      true →
        -1
      _ →
        let %t4 = FCmpGt(__x0, __x1)
        case %t4 of
          true →
            1
          _ →
            0

letrec __tc_Ord_Float_lt =
λ__x0, __x1.
    FCmpLt(__x0, __x1)

letrec __tc_Ord_Float_lte =
λ__x0, __x1.
    FCmpLe(__x0, __x1)

letrec __tc_Ord_Float_gt =
λ__x0, __x1.
    FCmpGt(__x0, __x1)

letrec __tc_Ord_Float_gte =
λ__x0, __x1.
    FCmpGe(__x0, __x1)

letrec __tc_Ord_String_compare =
λ__x0, __x1.
    let %t5 = Lt(__x0, __x1)
    case %t5 of
      true →
        -1
      _ →
        let %t6 = Gt(__x0, __x1)
        case %t6 of
          true →
            1
          _ →
            0

letrec __tc_Ord_String_lt =
λ__x0, __x1.
    Lt(__x0, __x1)

letrec __tc_Ord_String_lte =
λ__x0, __x1.
    Le(__x0, __x1)

letrec __tc_Ord_String_gt =
λ__x0, __x1.
    Gt(__x0, __x1)

letrec __tc_Ord_String_gte =
λ__x0, __x1.
    Ge(__x0, __x1)

letrec __tc_Num_Int_add =
λ__x0, __x1.
    IAdd(__x0, __x1)

letrec __tc_Num_Int_sub =
λ__x0, __x1.
    ISub(__x0, __x1)

letrec __tc_Num_Int_mul =
λ__x0, __x1.
    IMul(__x0, __x1)

letrec __tc_Num_Int_div =
λ__x0, __x1.
    IDiv(__x0, __x1)

letrec __tc_Num_Float_add =
λ__x0, __x1.
    FAdd(__x0, __x1)

letrec __tc_Num_Float_sub =
λ__x0, __x1.
    FSub(__x0, __x1)

letrec __tc_Num_Float_mul =
λ__x0, __x1.
    FMul(__x0, __x1)

letrec __tc_Num_Float_div =
λ__x0, __x1.
    FDiv(__x0, __x1)

letrec __tc_Show_Int_show =
λ__x0.
    ToString(__x0)

letrec __tc_Show_Float_show =
λ__x0.
    ToString(__x0)

letrec __tc_Show_String_show =
λ__x0.
    ToString(__x0)

letrec __tc_Show_Bool_show =
λ__x0.
    ToString(__x0)

letrec __tc_Semigroup_String_append =
λ__x0, __x1.
    string_concat(__x0, __x1)

letrec size =
λ__x0.
    Panic("No instance of Sizeable.size for the given type")

letrec eq =
λ__x0, __x1.
    Panic("No instance of Eq.eq for the given type")

letrec neq =
λ__x0, __x1.
    Panic("No instance of Eq.neq for the given type")

letrec compare =
λ__x0, __x1.
    Panic("No instance of Ord.compare for the given type")

letrec lt =
λ__x0, __x1.
    Panic("No instance of Ord.lt for the given type")

letrec lte =
λ__x0, __x1.
    Panic("No instance of Ord.lte for the given type")

letrec gt =
λ__x0, __x1.
    Panic("No instance of Ord.gt for the given type")

letrec gte =
λ__x0, __x1.
    Panic("No instance of Ord.gte for the given type")

letrec add =
λ__x0, __x1.
    Panic("No instance of Num.add for the given type")

letrec sub =
λ__x0, __x1.
    Panic("No instance of Num.sub for the given type")

letrec mul =
λ__x0, __x1.
    Panic("No instance of Num.mul for the given type")

letrec div =
λ__x0, __x1.
    Panic("No instance of Num.div for the given type")

letrec show =
λ__x0.
    Panic("No instance of Show.show for the given type")

letrec assert_failed =
λname.
    let %t7 = Add("ASSERTION FAILED: ", name)
    Println(%t7)

letrec assert_eq =
λa, b.
    let %t8 = CmpEq(a, b)
    case %t8 of
      true →
        MakeTuple()
      _ →
        let %t9 = assert_failed("assert_eq")
        let %t10 = Println(a)
        let %t11 = Println(b)
        Panic("assert_eq failed")

letrec assert_neq =
λa, b.
    let %t12 = CmpNe(a, b)
    case %t12 of
      true →
        MakeTuple()
      _ →
        let %t13 = assert_failed("assert_neq")
        Panic("assert_neq failed")

letrec assert_true =
λx.
    case x of
      true →
        MakeTuple()
      _ →
        Panic("assert_true failed")

letrec assert_false =
λx.
    let %t14 = Not(x)
    case %t14 of
      true →
        MakeTuple()
      _ →
        Panic("assert_false failed")

letrec assert_gt =
λa, b.
    let %t15 = Gt(a, b)
    case %t15 of
      true →
        MakeTuple()
      _ →
        Panic("assert_gt failed")

letrec assert_lt =
λa, b.
    let %t16 = Lt(a, b)
    case %t16 of
      true →
        MakeTuple()
      _ →
        Panic("assert_lt failed")

letrec assert_gte =
λa, b.
    let %t17 = Ge(a, b)
    case %t17 of
      true →
        MakeTuple()
      _ →
        Panic("assert_gte failed")

letrec assert_lte =
λa, b.
    let %t18 = Le(a, b)
    case %t18 of
      true →
        MakeTuple()
      _ →
        Panic("assert_lte failed")

letrec list_length =
λxs, acc.
    case xs of
      ::(h, t) →
        let %t19 = Add(acc, 1)
        list_length(t, %t19)
      _ →
        acc

letrec assert_len =
λcollection, expected.
    let actual = (let %t20 = IsList(collection)
    case %t20 of
      true →
        list_length(collection, 0)
      _ →
        Len(collection))
    let %t21 = Eq(actual, expected)
    case %t21 of
      true →
        MakeTuple()
      _ →
        let %t22 = assert_failed("assert_len")
        Panic("assert_len failed")

letrec assert_msg =
λcondition, message.
    case condition of
      true →
        MakeTuple()
      _ →
        Panic(message)

letrec map =
λarr, f.
    letrec map_go = (λidx, acc.
      let %t23 = (let %t24 = Len(arr)
      Ge(idx, %t24))
      case %t23 of
        true →
          acc
        _ →
          let %t25 = Index(arr, idx)
          case %t25 of
            Some(v) →
              let %t26 = Add(idx, 1)
              let %t27 = (let %t28 = f(v)
              push(acc, %t28))
              map_go(%t26, %t27)
            _ →
              let %t29 = Add(idx, 1)
              map_go(%t29, acc))
    let %t30 = MakeArray()
    map_go(0, %t30)

letrec filter =
λarr, pred.
    letrec filter_go = (λidx, acc.
      let %t31 = (let %t32 = Len(arr)
      Ge(idx, %t32))
      case %t31 of
        true →
          acc
        _ →
          let %t33 = Index(arr, idx)
          case %t33 of
            Some(v) →
              let %t34 = pred(v)
              case %t34 of
                true →
                  let %t35 = Add(idx, 1)
                  let %t36 = push(acc, v)
                  filter_go(%t35, %t36)
                _ →
                  let %t37 = Add(idx, 1)
                  filter_go(%t37, acc)
            _ →
              let %t38 = Add(idx, 1)
              filter_go(%t38, acc))
    let %t39 = MakeArray()
    filter_go(0, %t39)

letrec fold =
λarr, acc, f.
    letrec fold_go = (λidx, a.
      let %t40 = (let %t41 = Len(arr)
      Ge(idx, %t41))
      case %t40 of
        true →
          a
        _ →
          let %t42 = Index(arr, idx)
          case %t42 of
            Some(v) →
              let %t43 = Add(idx, 1)
              let %t44 = f(a, v)
              fold_go(%t43, %t44)
            _ →
              let %t45 = Add(idx, 1)
              fold_go(%t45, a))
    fold_go(0, acc)

letrec each =
λarr, f.
    letrec each_go = (λidx.
      let %t46 = (let %t47 = Len(arr)
      Ge(idx, %t47))
      case %t46 of
        true →
          MakeTuple()
        _ →
          let %t48 = Index(arr, idx)
          case %t48 of
            Some(v) →
              let %t49 = f(v)
              let %t50 = Add(idx, 1)
              each_go(%t50)
            _ →
              let %t51 = Add(idx, 1)
              each_go(%t51))
    each_go(0)

letrec any =
λarr, pred.
    letrec any_go = (λidx.
      let %t52 = (let %t53 = Len(arr)
      Ge(idx, %t53))
      case %t52 of
        true →
          false
        _ →
          let %t54 = Index(arr, idx)
          case %t54 of
            Some(v) →
              let %t55 = pred(v)
              case %t55 of
                true →
                  true
                _ →
                  let %t56 = Add(idx, 1)
                  any_go(%t56)
            _ →
              let %t57 = Add(idx, 1)
              any_go(%t57))
    any_go(0)

letrec all =
λarr, pred.
    letrec all_go = (λidx.
      let %t58 = (let %t59 = Len(arr)
      ICmpGe(idx, %t59))
      case %t58 of
        true →
          true
        _ →
          let %t60 = Index(arr, idx)
          case %t60 of
            Some(v) →
              let %t61 = pred(v)
              case %t61 of
                true →
                  let %t62 = Add(idx, 1)
                  all_go(%t62)
                _ →
                  false
            _ →
              let %t63 = Add(idx, 1)
              all_go(%t63))
    all_go(0)

letrec find =
λarr, pred.
    letrec find_go = (λidx.
      let %t64 = (let %t65 = Len(arr)
      Ge(idx, %t65))
      case %t64 of
        true →
          None
        _ →
          let %t66 = Index(arr, idx)
          case %t66 of
            Some(v) →
              let %t67 = pred(v)
              case %t67 of
                true →
                  Some(v)
                _ →
                  let %t68 = Add(idx, 1)
                  find_go(%t68)
            _ →
              let %t69 = Add(idx, 1)
              find_go(%t69))
    find_go(0)

letrec count =
λarr, pred.
    letrec count_go = (λidx, n.
      let %t70 = (let %t71 = Len(arr)
      Ge(idx, %t71))
      case %t70 of
        true →
          n
        _ →
          let %t72 = Index(arr, idx)
          case %t72 of
            Some(v) →
              let %t73 = pred(v)
              case %t73 of
                true →
                  let %t74 = Add(idx, 1)
                  let %t75 = Add(n, 1)
                  count_go(%t74, %t75)
                _ →
                  let %t76 = Add(idx, 1)
                  count_go(%t76, n)
            _ →
              let %t77 = Add(idx, 1)
              count_go(%t77, n))
    count_go(0, 0)

letrec flat_map =
λarr, f.
    letrec flat_map_go = (λidx, acc.
      let %t78 = (let %t79 = Len(arr)
      Ge(idx, %t79))
      case %t78 of
        true →
          acc
        _ →
          let %t80 = Index(arr, idx)
          case %t80 of
            Some(v) →
              let %t81 = Add(idx, 1)
              let %t82 = (let %t83 = f(v)
              concat(acc, %t83))
              flat_map_go(%t81, %t82)
            _ →
              let %t84 = Add(idx, 1)
              flat_map_go(%t84, acc))
    let %t85 = MakeArray()
    flat_map_go(0, %t85)

letrec flatten =
λarr.
    letrec flatten_go = (λidx, acc.
      let %t86 = (let %t87 = Len(arr)
      Ge(idx, %t87))
      case %t86 of
        true →
          acc
        _ →
          let %t88 = Index(arr, idx)
          case %t88 of
            Some(v) →
              let %t89 = Add(idx, 1)
              let %t90 = concat(acc, v)
              flatten_go(%t89, %t90)
            _ →
              let %t91 = Add(idx, 1)
              flatten_go(%t91, acc))
    let %t92 = MakeArray()
    flatten_go(0, %t92)

letrec zip =
λa_arr, b_arr.
    let min_len = (let %t93 = (let %t94 = Len(a_arr)
    let %t95 = Len(b_arr)
    Lt(%t94, %t95))
    case %t93 of
      true →
        Len(a_arr)
      _ →
        Len(b_arr))
    letrec zip_go = (λidx, acc.
      let %t96 = Ge(idx, min_len)
      case %t96 of
        true →
          acc
        _ →
          let %t97 = Index(a_arr, idx)
          case %t97 of
            Some(va) →
              let %t98 = Index(b_arr, idx)
              case %t98 of
                Some(vb) →
                  let %t99 = Add(idx, 1)
                  let %t100 = (let %t101 = MakeTuple(va, vb)
                  push(acc, %t101))
                  zip_go(%t99, %t100)
                _ →
                  acc
            _ →
              acc)
    let %t102 = MakeArray()
    zip_go(0, %t102)

letrec contains =
λarr, x.
    let %t103 = (λv.
      ICmpEq(v, x))
    any(arr, %t103)

letrec concat =
λleft, right.
    ArrayConcat(left, right)

letrec get =
λarr, i.
    array_get(arr, i)

letrec length =
λarr.
    array_len(arr)

letrec push =
λarr, value.
    ArrayPush(arr, value)

letrec reverse =
λarr.
    let %t104 = (let %t105 = Len(arr)
    Eq(%t105, 0))
    case %t104 of
      true →
        MakeArray()
      _ →
        let %t106 = Index(arr, 0)
        case %t106 of
          Some(v) →
            let %t107 = (let %t108 = (let %t109 = Len(arr)
            slice(arr, 1, %t109))
            reverse(%t108))
            let %t110 = MakeArray(v)
            concat(%t107, %t110)
          _ →
            MakeArray()

letrec slice =
λarr, start, end.
    ArraySlice(arr, start, end)

letrec update =
λarr, index, value.
    array_set(arr, index, value)

letrec sum =
λarr.
    let %t111 = (λa, x.
      Add(a, x))
    fold(arr, 0, %t111)

letrec product =
λarr.
    let %t112 = (λa, x.
      Mul(a, x))
    fold(arr, 1, %t112)

letrec maximum =
λarr.
    letrec maximum_go = (λi, best.
      let %t113 = (let %t114 = Len(arr)
      Ge(i, %t114))
      case %t113 of
        true →
          best
        _ →
          let %t115 = Index(arr, i)
          case %t115 of
            Some(v) →
              let %t116 = Add(i, 1)
              let %t117 = (let %t118 = Gt(v, best)
              case %t118 of
                true →
                  v
                _ →
                  best)
              maximum_go(%t116, %t117)
            _ →
              let %t119 = Add(i, 1)
              maximum_go(%t119, best))
    let %t120 = (let %t121 = Len(arr)
    Eq(%t121, 0))
    case %t120 of
      true →
        0
      _ →
        let %t122 = Index(arr, 0)
        case %t122 of
          Some(first) →
            maximum_go(1, first)
          _ →
            0

letrec minimum =
λarr.
    letrec minimum_go = (λi, best.
      let %t123 = (let %t124 = Len(arr)
      Ge(i, %t124))
      case %t123 of
        true →
          best
        _ →
          let %t125 = Index(arr, i)
          case %t125 of
            Some(v) →
              let %t126 = Add(i, 1)
              let %t127 = (let %t128 = Lt(v, best)
              case %t128 of
                true →
                  v
                _ →
                  best)
              minimum_go(%t126, %t127)
            _ →
              let %t129 = Add(i, 1)
              minimum_go(%t129, best))
    let %t130 = (let %t131 = Len(arr)
    Eq(%t131, 0))
    case %t130 of
      true →
        0
      _ →
        let %t132 = Index(arr, 0)
        case %t132 of
          Some(first) →
            minimum_go(1, first)
          _ →
            0

letrec first =
λarr.
    Index(arr, 0)

letrec last =
λarr.
    let n = Len(arr)
    let %t133 = Eq(n, 0)
    case %t133 of
      true →
        None
      _ →
        let %t134 = Sub(n, 1)
        Index(arr, %t134)

letrec range =
λstart, stop.
    letrec range_go = (λi, acc.
      let %t135 = Ge(i, stop)
      case %t135 of
        true →
          acc
        _ →
          let %t136 = Add(i, 1)
          let %t137 = push(acc, i)
          range_go(%t136, %t137))
    let %t138 = MakeArray()
    range_go(start, %t138)

letrec sort =
λarr.
    let %t139 = (λv.
      v)
    sort_by(arr, %t139)

letrec sort_by =
λarr, f.
    letrec insert_tagged = (λsorted, pair, idx.
      let %t140 = (let %t141 = Len(sorted)
      Ge(idx, %t141))
      case %t140 of
        true →
          push(sorted, pair)
        _ →
          let %t142 = Index(sorted, idx)
          case %t142 of
            Some(current) →
              let %t143 = (let %t144 = pair.0
              let %t145 = current.0
              Le(%t144, %t145))
              case %t143 of
                true →
                  let left = slice(sorted, 0, idx)
                  let right = (let %t146 = Len(sorted)
                  slice(sorted, idx, %t146))
                  let %t147 = (let %t148 = MakeArray(pair)
                  concat(left, %t148))
                  concat(%t147, right)
                _ →
                  let %t149 = Add(idx, 1)
                  insert_tagged(sorted, pair, %t149)
            _ →
              push(sorted, pair))
    letrec sort_by_go = (λidx, pairs.
      let %t150 = (let %t151 = Len(arr)
      Ge(idx, %t151))
      case %t150 of
        true →
          pairs
        _ →
          let %t152 = Index(arr, idx)
          case %t152 of
            Some(v) →
              let %t153 = Add(idx, 1)
              let %t154 = (let %t155 = (let %t156 = f(v)
              MakeTuple(%t156, v))
              insert_tagged(pairs, %t155, 0))
              sort_by_go(%t153, %t154)
            _ →
              let %t157 = Add(idx, 1)
              sort_by_go(%t157, pairs))
    let %t158 = (let %t159 = MakeArray()
    sort_by_go(0, %t159))
    let %t160 = (λp.
      p.1)
    map(%t158, %t160)

letrec take =
λarr, n.
    let %t161 = (let %t162 = (let %t163 = Len(arr)
    Gt(n, %t163))
    case %t162 of
      true →
        Len(arr)
      _ →
        n)
    slice(arr, 0, %t161)

letrec drop =
λarr, n.
    let %t164 = (let %t165 = (let %t166 = Len(arr)
    Gt(n, %t166))
    case %t165 of
      true →
        Len(arr)
      _ →
        n)
    let %t167 = Len(arr)
    slice(arr, %t164, %t167)

letrec swap =
λarr, i, j.
    let %t168 = Index(arr, i)
    case %t168 of
      Some(vi) →
        let %t169 = Index(arr, j)
        case %t169 of
          Some(vj) →
            let %t170 = update(arr, i, vj)
            update(%t170, j, vi)
          _ →
            arr
      _ →
        arr

letrec enumerate =
λarr.
    letrec enumerate_go = (λidx, acc.
      let %t171 = (let %t172 = Len(arr)
      Ge(idx, %t172))
      case %t171 of
        true →
          acc
        _ →
          let %t173 = Index(arr, idx)
          case %t173 of
            Some(v) →
              let %t174 = IAdd(idx, 1)
              let %t175 = (let %t176 = MakeTuple(idx, v)
              push(acc, %t176))
              enumerate_go(%t174, %t175)
            _ →
              let %t177 = Add(idx, 1)
              enumerate_go(%t177, acc))
    let %t178 = MakeArray()
    enumerate_go(0, %t178)

letrec tabulate =
λn, f.
    letrec tabulate_go = (λi, acc.
      let %t179 = Ge(i, n)
      case %t179 of
        true →
          acc
        _ →
          let %t180 = Add(i, 1)
          let %t181 = (let %t182 = f(i)
          push(acc, %t182))
          tabulate_go(%t180, %t181))
    let %t183 = MakeArray()
    tabulate_go(0, %t183)

letrec from_list =
λxs.
    ToArray(xs)

letrec find_index =
λarr, pred.
    letrec find_index_go = (λidx.
      let %t184 = (let %t185 = Len(arr)
      Ge(idx, %t185))
      case %t184 of
        true →
          None
        _ →
          let %t186 = Index(arr, idx)
          case %t186 of
            Some(v) →
              let %t187 = pred(v)
              case %t187 of
                true →
                  Some(idx)
                _ →
                  let %t188 = Add(idx, 1)
                  find_index_go(%t188)
            _ →
              let %t189 = Add(idx, 1)
              find_index_go(%t189))
    find_index_go(0)

letrec each_indexed =
λarr, f.
    letrec each_indexed_go = (λidx.
      let %t190 = (let %t191 = Len(arr)
      Ge(idx, %t191))
      case %t190 of
        true →
          MakeTuple()
        _ →
          let %t192 = Index(arr, idx)
          case %t192 of
            Some(v) →
              let %t193 = f(idx, v)
              let %t194 = Add(idx, 1)
              each_indexed_go(%t194)
            _ →
              let %t195 = Add(idx, 1)
              each_indexed_go(%t195))
    each_indexed_go(0)

letrec update_many =
λarr, pairs.
    letrec update_many_go = (λa, ps.
      case ps of
        ::(p, rest) →
          let %t196 = (let %t197 = p.0
          let %t198 = p.1
          update(a, %t197, %t198))
          update_many_go(%t196, rest)
        _ →
          a)
    update_many_go(arr, pairs)

letrec accum =
λsize, init, pairs, f.
    let base = (let %t199 = (λ_.
      init)
    tabulate(size, %t199))
    letrec accum_go = (λa, ps.
      case ps of
        ::(p, rest) →
          let %t200 = (let %t201 = p.0
          Index(a, %t201))
          case %t200 of
            Some(cur) →
              let %t202 = (let %t203 = p.0
              let %t204 = (let %t205 = p.1
              f(cur, %t205))
              update(a, %t203, %t204))
              accum_go(%t202, rest)
            _ →
              accum_go(a, rest)
        _ →
          a)
    accum_go(base, pairs)

letrec to_list =
λarr.
    letrec to_list_go = (λidx.
      let %t206 = (let %t207 = Len(arr)
      Ge(idx, %t207))
      case %t206 of
        true →
          []
        _ →
          let %t208 = Index(arr, idx)
          case %t208 of
            Some(v) →
              let %t209 = (let %t210 = Add(idx, 1)
              to_list_go(%t210))
              ::(v, %t209)
            _ →
              let %t211 = Add(idx, 1)
              to_list_go(%t211))
    to_list_go(0)

letrec print_all =
λarr.
    letrec pa_go = (λi.
      let %t212 = (let %t213 = Len(arr)
      Lt(i, %t213))
      case %t212 of
        true →
          let %t214 = (let %t215 = Index(arr, i)
          case %t215 of
            Some(v) →
              Print(v)
            _ →
              Print(""))
          let %t216 = Add(i, 1)
          pa_go(%t216)
        _ →
          MakeTuple())
    pa_go(0)

letrec println_all =
λarr.
    letrec pla_go = (λi.
      let %t217 = (let %t218 = Len(arr)
      Lt(i, %t218))
      case %t217 of
        true →
          let %t219 = (let %t220 = Index(arr, i)
          case %t220 of
            Some(v) →
              Println(v)
            _ →
              Println(""))
          let %t221 = Add(i, 1)
          pla_go(%t221)
        _ →
          MakeTuple())
    pla_go(0)

letrec read_lines =
λpath.
    let content = ReadFile(path)
    Split(content, "\n")

letrec parse_ints =
λarr.
    letrec pi_go = (λi, result.
      let %t222 = (let %t223 = Len(arr)
      Ge(i, %t223))
      case %t222 of
        true →
          result
        _ →
          let %t224 = Index(arr, i)
          case %t224 of
            Some(s) →
              let %t225 = Add(i, 1)
              let %t226 = (let %t227 = Array.push
              let %t228 = ParseInt(s)
              %t227(result, %t228))
              pi_go(%t225, %t226)
            _ →
              let %t229 = Add(i, 1)
              pi_go(%t229, result))
    let %t230 = MakeArray()
    pi_go(0, %t230)

letrec split_ints =
λs, sep.
    let parts = Split(s, sep)
    parse_ints(parts)

letrec map =
λxs, f.
    letrec map_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t231 = (let %t232 = f(h)
          ::(%t232, acc))
          map_acc(t, %t231)
        _ →
          reverse(acc))
    let %t233 = []
    map_acc(xs, %t233)

letrec filter =
λxs, pred.
    letrec filter_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t234 = pred(h)
          case %t234 of
            true →
              let %t235 = ::(h, acc)
              filter_acc(t, %t235)
            _ →
              filter_acc(t, acc)
        _ →
          reverse(acc))
    let %t236 = []
    filter_acc(xs, %t236)

letrec fold =
λxs, acc, f.
    letrec fold_list = (λys, a.
      case ys of
        ::(h, t) →
          let %t237 = f(a, h)
          fold_list(t, %t237)
        _ →
          a)
    fold_list(xs, acc)

letrec foldr =
λxs, acc, f.
    case xs of
      ::(h, t) →
        let %t238 = foldr(t, acc, f)
        f(h, %t238)
      _ →
        acc

letrec fold1 =
λxs, f.
    case xs of
      ::(h, t) →
        fold(t, h, f)
      _ →
        Panic("fold1 called on empty list")

letrec flat_map =
λxs, f.
    letrec flat_map_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t239 = (let %t240 = (let %t241 = f(h)
          reverse(%t241))
          __tc_Semigroup_String_append(%t240, acc))
          flat_map_acc(t, %t239)
        _ →
          reverse(acc))
    let %t242 = []
    flat_map_acc(xs, %t242)

letrec flatten =
λxs.
    letrec flatten_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t243 = (let %t244 = reverse(h)
          append(%t244, acc))
          flatten_acc(t, %t243)
        _ →
          reverse(acc))
    let %t245 = []
    flatten_acc(xs, %t245)

letrec concat =
λxss.
    flatten(xss)

letrec any =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t246 = pred(h)
        case %t246 of
          true →
            true
          _ →
            any(t, pred)
      _ →
        false

letrec all =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t247 = pred(h)
        case %t247 of
          true →
            all(t, pred)
          _ →
            false
      _ →
        true

letrec find =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t248 = pred(h)
        case %t248 of
          true →
            Some(h)
          _ →
            find(t, pred)
      _ →
        None

letrec count =
λxs, pred.
    letrec count_go = (λys, n.
      case ys of
        ::(h, t) →
          let %t249 = pred(h)
          case %t249 of
            true →
              let %t250 = IAdd(n, 1)
              count_go(t, %t250)
            _ →
              count_go(t, n)
        _ →
          n)
    count_go(xs, 0)

letrec each =
λxs, f.
    case xs of
      ::(h, t) →
        let %t251 = f(h)
        let %t252 = Flow.List.each
        %t252(t, f)
      _ →
        MakeTuple()

letrec take =
λxs, n.
    letrec take_acc = (λys, remaining, acc.
      let %t253 = ICmpLe(remaining, 0)
      case %t253 of
        true →
          reverse(acc)
        _ →
          case ys of
            ::(h, t) →
              let %t254 = ISub(remaining, 1)
              let %t255 = ::(h, acc)
              take_acc(t, %t254, %t255)
            _ →
              reverse(acc))
    let %t256 = []
    take_acc(xs, n, %t256)

letrec drop =
λxs, n.
    let %t257 = ICmpLe(n, 0)
    case %t257 of
      true →
        xs
      _ →
        case xs of
          ::(h, t) →
            let %t258 = ISub(n, 1)
            drop(t, %t258)
          _ →
            []

letrec take_while =
λxs, pred.
    letrec take_while_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t259 = pred(h)
          case %t259 of
            true →
              let %t260 = ::(h, acc)
              take_while_acc(t, %t260)
            _ →
              reverse(acc)
        _ →
          reverse(acc))
    let %t261 = []
    take_while_acc(xs, %t261)

letrec drop_while =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t262 = pred(h)
        case %t262 of
          true →
            drop_while(t, pred)
          _ →
            xs
      _ →
        []

letrec split_at =
λxs, n.
    let %t263 = ICmpLe(n, 0)
    case %t263 of
      true →
        let %t264 = []
        MakeTuple(%t264, xs)
      _ →
        case xs of
          ::(h, t) →
            let %t265 = (let %t266 = ISub(n, 1)
            split_at(t, %t266))
            case %t265 of
              (left, right) →
                let %t267 = ::(h, left)
                MakeTuple(%t267, right)
              _ →
                let %t268 = []
                MakeTuple(%t268, xs)
          _ →
            let %t269 = []
            let %t270 = []
            MakeTuple(%t269, %t270)

letrec span =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t271 = pred(h)
        case %t271 of
          true →
            let %t272 = span(t, pred)
            case %t272 of
              (left, right) →
                let %t273 = ::(h, left)
                MakeTuple(%t273, right)
              _ →
                let %t274 = []
                MakeTuple(%t274, xs)
          _ →
            let %t275 = []
            MakeTuple(%t275, xs)
      _ →
        let %t276 = []
        let %t277 = []
        MakeTuple(%t276, %t277)

letrec scanl =
λxs, acc, f.
    let %t278 = scanl_rest(xs, acc, f)
    ::(acc, %t278)

letrec scanr =
λxs, acc, f.
    let %t279 = scanr_go(xs, acc, f)
    case %t279 of
      (result, last_acc) →
        result
      _ →
        MakeList(acc)

letrec sort =
λxs.
    let %t280 = (λx.
      x)
    merge_sort_by(xs, %t280)

letrec sort_by =
λxs, f.
    merge_sort_by(xs, f)

letrec first =
λxs.
    case xs of
      ::(h, t) →
        h
      _ →
        None

letrec last =
λxs.
    case xs of
      ::(h, t) →
        case t of
          ::(h2, t2) →
            last(t)
          _ →
            h
      _ →
        None

letrec rest =
λxs.
    case xs of
      ::(h, t) →
        t
      _ →
        []

letrec length =
λxs.
    letrec length_go = (λys, acc.
      case ys of
        ::(h, t) →
          let %t281 = IAdd(acc, 1)
          length_go(t, %t281)
        _ →
          acc)
    length_go(xs, 0)

letrec init =
λxs.
    case xs of
      ::(h, t) →
        case t of
          ::(h2, t2) →
            let %t282 = init(t)
            ::(h, %t282)
          _ →
            []
      _ →
        Panic("init called on empty list")

letrec null =
λxs.
    case xs of
      ::(h, t) →
        false
      _ →
        true

letrec range =
λstart, stop.
    letrec range_go = (λcurrent, acc.
      let %t283 = ICmpGe(current, stop)
      case %t283 of
        true →
          reverse(acc)
        _ →
          let %t284 = IAdd(current, 1)
          let %t285 = ::(current, acc)
          range_go(%t284, %t285))
    let %t286 = []
    range_go(start, %t286)

letrec reverse =
λxs.
    letrec rev_acc = (λys, acc.
      case ys of
        ::(h, t) →
          let %t287 = ::(h, acc)
          rev_acc(t, %t287)
        _ →
          acc)
    let %t288 = []
    rev_acc(xs, %t288)

letrec zip =
λxs, ys.
    let %t289 = (let %t290 = []
    zip_go(xs, ys, %t290))
    reverse(%t289)

letrec zip_with =
λxs, ys, f.
    let %t291 = (let %t292 = []
    zip_with_go(xs, ys, f, %t292))
    reverse(%t291)

letrec unzip =
λxs.
    case xs of
      ::((a, b), t) →
        let %t293 = unzip(t)
        case %t293 of
          (left, right) →
            let %t294 = ::(a, left)
            let %t295 = ::(b, right)
            MakeTuple(%t294, %t295)
          _ →
            let %t296 = []
            let %t297 = []
            MakeTuple(%t296, %t297)
      _ →
        let %t298 = []
        let %t299 = []
        MakeTuple(%t298, %t299)

letrec enumerate =
λxs.
    enumerate_go(xs, 0)

letrec contains =
λxs, x.
    case xs of
      ::(h, t) →
        let %t300 = eq(h, x)
        case %t300 of
          true →
            true
          _ →
            contains(t, x)
      _ →
        false

letrec nub =
λxs.
    let %t301 = (λx.
      x)
    unique_by(xs, %t301)

letrec partition =
λxs, pred.
    case xs of
      ::(h, t) →
        let %t302 = partition(t, pred)
        case %t302 of
          (yes, no) →
            let %t303 = pred(h)
            case %t303 of
              true →
                let %t304 = ::(h, yes)
                MakeTuple(%t304, no)
              _ →
                let %t305 = ::(h, no)
                MakeTuple(yes, %t305)
          _ →
            let %t306 = []
            let %t307 = []
            MakeTuple(%t306, %t307)
      _ →
        let %t308 = []
        let %t309 = []
        MakeTuple(%t308, %t309)

letrec delete =
λxs, x.
    delete_go(xs, x)

letrec delete_go =
λxs, x.
    let %t310 = []
    delete_go_acc(xs, x, %t310)

letrec delete_go_acc =
λxs, x, acc.
    case xs of
      ::(h, t) →
        let %t311 = Eq(h, x)
        case %t311 of
          true →
            let %t312 = reverse(acc)
            let %t313 = clone_list(t)
            append(%t312, %t313)
          _ →
            let %t314 = ::(h, acc)
            delete_go_acc(t, x, %t314)
      _ →
        reverse(acc)

letrec clone_list =
λxs.
    case xs of
      ::(h, t) →
        let %t315 = clone_list(t)
        ::(h, %t315)
      _ →
        []

letrec intersperse =
λxs, sep.
    case xs of
      ::(h, t) →
        let %t316 = intersperse_rest(t, sep)
        ::(h, %t316)
      _ →
        []

letrec intercalate =
λxss, sep.
    let %t317 = intersperse(xss, sep)
    concat(%t317)

letrec is_prefix =
λprefix, xs.
    case prefix of
      ::(p, pt) →
        case xs of
          ::(x, xt) →
            let %t318 = eq(p, x)
            case %t318 of
              true →
                is_prefix(pt, xt)
              _ →
                false
          _ →
            false
      _ →
        true

letrec is_suffix =
λsuffix, xs.
    let %t319 = reverse(suffix)
    let %t320 = reverse(xs)
    is_prefix(%t319, %t320)

letrec nth =
λxs, n.
    let %t321 = ICmpLt(n, 0)
    case %t321 of
      true →
        None
      _ →
        case xs of
          ::(h, t) →
            let %t322 = ICmpEq(n, 0)
            case %t322 of
              true →
                Some(h)
              _ →
                let %t323 = ISub(n, 1)
                nth(t, %t323)
          _ →
            None

letrec replicate =
λn, x.
    let %t324 = ICmpLe(n, 0)
    case %t324 of
      true →
        []
      _ →
        let %t325 = (let %t326 = ISub(n, 1)
        replicate(%t326, x))
        ::(x, %t325)

letrec iterate =
λx, f, n.
    let %t327 = ICmpLe(n, 0)
    case %t327 of
      true →
        []
      _ →
        let %t328 = (let %t329 = f(x)
        let %t330 = ISub(n, 1)
        iterate(%t329, f, %t330))
        ::(x, %t328)

letrec unfold =
λseed, f.
    let %t331 = f(seed)
    case %t331 of
      Some(step) →
        let %t332 = step.0
        let %t333 = (let %t334 = step.1
        unfold(%t334, f))
        ::(%t332, %t333)
      _ →
        []

letrec group_by =
λxs, eq.
    case xs of
      ::(h, t) →
        group_by_head(h, t, eq)
      _ →
        []

letrec unique_by =
λxs, f.
    let %t335 = (let %t336 = []
    let %t337 = []
    unique_by_go(xs, f, %t336, %t337))
    reverse(%t335)

letrec maximum =
λxs.
    case xs of
      ::(h, t) →
        let %t338 = maximum_go(t, h)
        Some(%t338)
      _ →
        None

letrec minimum =
λxs.
    case xs of
      ::(h, t) →
        let %t339 = minimum_go(t, h)
        Some(%t339)
      _ →
        None

letrec sum =
λxs.
    letrec sum_go = (λys, acc.
      case ys of
        ::(h, t) →
          let %t340 = IAdd(acc, h)
          sum_go(t, %t340)
        _ →
          acc)
    sum_go(xs, 0)

letrec product =
λxs.
    letrec product_go = (λys, acc.
      case ys of
        ::(h, t) →
          let %t341 = IMul(acc, h)
          product_go(t, %t341)
        _ →
          acc)
    product_go(xs, 1)

letrec append =
λxs, ys.
    case xs of
      ::(h, t) →
        let %t342 = append(t, ys)
        ::(h, %t342)
      _ →
        ys

letrec enumerate_go =
λxs, i.
    case xs of
      ::(h, t) →
        let %t343 = MakeTuple(i, h)
        let %t344 = (let %t345 = IAdd(i, 1)
        enumerate_go(t, %t345))
        ::(%t343, %t344)
      _ →
        []

letrec intersperse_rest =
λxs, sep.
    case xs of
      ::(h, t) →
        let %t346 = (let %t347 = intersperse_rest(t, sep)
        ::(h, %t347))
        ::(sep, %t346)
      _ →
        []

letrec intercalate_rest =
λxss, sep.
    case xss of
      ::(h, t) →
        let %t348 = (let %t349 = intercalate_rest(t, sep)
        append(h, %t349))
        append(sep, %t348)
      _ →
        []

letrec scanl_rest =
λxs, acc, f.
    case xs of
      ::(h, t) →
        let %t350 = f(acc, h)
        scanl_emit(t, %t350, f)
      _ →
        []

letrec scanl_emit =
λxs, next, f.
    let %t351 = scanl_rest(xs, next, f)
    ::(next, %t351)

letrec unique_by_go =
λxs, f, seen, acc.
    case xs of
      ::(h, t) →
        let %t352 = f(h)
        unique_by_step(t, h, %t352, f, seen, acc)
      _ →
        acc

letrec unique_by_step =
λxs, value, key, f, seen, acc.
    let %t353 = contains(seen, key)
    case %t353 of
      true →
        unique_by_go(xs, f, seen, acc)
      _ →
        let %t354 = ::(key, seen)
        let %t355 = ::(value, acc)
        unique_by_go(xs, f, %t354, %t355)

letrec scanr_go =
λxs, acc, f.
    case xs of
      ::(h, t) →
        let %t356 = scanr_go(t, acc, f)
        scanr_step(h, %t356, acc, f)
      _ →
        let %t357 = MakeList(acc)
        MakeTuple(%t357, acc)

letrec scanr_step =
λh, state, acc, f.
    case state of
      (rest, next_acc) →
        scanr_finish(rest, next_acc, h, f)
      _ →
        let %t358 = MakeList(acc)
        MakeTuple(%t358, acc)

letrec scanr_finish =
λrest, next_acc, h, f.
    let current = f(h, next_acc)
    let %t359 = ::(current, rest)
    MakeTuple(%t359, current)

letrec merge_sort_by =
λxs, key_fn.
    case xs of
      ::(_, t) →
        case t of
          ::(_, _) →
            merge_sort_do(xs, key_fn)
          _ →
            xs
      _ →
        xs

letrec merge_sort_do =
λxs, key_fn.
    let mid = (let %t360 = length(xs)
    IDiv(%t360, 2))
    let %t361 = split_at(xs, mid)
    case %t361 of
      (left, right) →
        let %t362 = merge_sort_by(left, key_fn)
        let %t363 = merge_sort_by(right, key_fn)
        merge_by_key(%t362, %t363, key_fn)
      _ →
        []

letrec merge_by_key =
λleft, right, key_fn.
    case left of
      ::(a, at) →
        case right of
          ::(b, bt) →
            let %t364 = (let %t365 = key_fn(a)
            let %t366 = key_fn(b)
            Le(%t365, %t366))
            case %t364 of
              true →
                let %t367 = merge_by_key(at, right, key_fn)
                ::(a, %t367)
              _ →
                let %t368 = merge_by_key(left, bt, key_fn)
                ::(b, %t368)
          _ →
            left
      _ →
        right

letrec zip_go =
λxs, ys, acc.
    case xs of
      ::(a, at) →
        case ys of
          ::(b, bt) →
            let %t369 = (let %t370 = MakeTuple(a, b)
            ::(%t370, acc))
            zip_go(at, bt, %t369)
          _ →
            acc
      _ →
        acc

letrec zip_with_go =
λxs, ys, f, acc.
    case xs of
      ::(a, at) →
        case ys of
          ::(b, bt) →
            let %t371 = (let %t372 = f(a, b)
            ::(%t372, acc))
            zip_with_go(at, bt, f, %t371)
          _ →
            acc
      _ →
        acc

letrec group_by_head =
λh, t, eq.
    let %t373 = (let %t374 = (λx.
      eq(h, x))
    span(t, %t374))
    case %t373 of
      (prefix, rest) →
        let %t375 = ::(h, prefix)
        let %t376 = group_by(rest, eq)
        ::(%t375, %t376)
      _ →
        let %t377 = MakeList(h)
        let %t378 = group_by(t, eq)
        ::(%t377, %t378)

letrec maximum_go =
λxs, best.
    case xs of
      ::(h, t) →
        let %t379 = (let %t380 = Gt(h, best)
        case %t380 of
          true →
            h
          _ →
            best)
        maximum_go(t, %t379)
      _ →
        best

letrec minimum_go =
λxs, best.
    case xs of
      ::(h, t) →
        let %t381 = (let %t382 = Lt(h, best)
        case %t382 of
          true →
            h
          _ →
            best)
        minimum_go(t, %t381)
      _ →
        best

letrec sqrt =
λx.
    FSqrt(x)

letrec sin =
λx.
    FSin(x)

letrec cos =
λx.
    FCos(x)

letrec exp =
λx.
    FExp(x)

letrec log =
λx.
    FLog(x)

letrec floor =
λx.
    FFloor(x)

letrec ceil =
λx.
    FCeil(x)

letrec round =
λx.
    FRound(x)

letrec unwrap =
λopt.
    case opt of
      Some(v) →
        v
      _ →
        Panic("unwrap called on None")

letrec unwrap_or =
λopt, default.
    case opt of
      Some(v) →
        v
      _ →
        default

letrec map_option =
λopt, f.
    case opt of
      Some(v) →
        let %t383 = f(v)
        Some(%t383)
      _ →
        None

letrec flat_map_option =
λopt, f.
    case opt of
      Some(v) →
        f(v)
      _ →
        None

letrec is_some =
λopt.
    case opt of
      Some(_) →
        true
      _ →
        false

letrec is_none_opt =
λopt.
    case opt of
      Some(_) →
        false
      _ →
        true

letrec starts_with =
λs, prefix.
    let plen = Len(prefix)
    let %t384 = (let %t385 = Len(s)
    ICmpGt(plen, %t385))
    case %t384 of
      true →
        false
      _ →
        let %t386 = Substring(s, 0, plen)
        Eq(%t386, prefix)

letrec ends_with =
λs, suffix.
    let slen = Len(s)
    let plen = Len(suffix)
    let %t387 = Gt(plen, slen)
    case %t387 of
      true →
        false
      _ →
        let %t388 = (let %t389 = ISub(slen, plen)
        Substring(s, %t389, slen))
        Eq(%t388, suffix)

letrec chars =
λs.
    letrec chars_go = (λi, result.
      let %t390 = (let %t391 = Len(s)
      ICmpGe(i, %t391))
      case %t390 of
        true →
          result
        _ →
          let %t392 = IAdd(i, 1)
          let %t393 = (let %t394 = Array.push
          let %t395 = (let %t396 = IAdd(i, 1)
          Substring(s, i, %t396))
          %t394(result, %t395))
          chars_go(%t392, %t393))
    let %t397 = MakeArray()
    chars_go(0, %t397)

letrec join =
λarr, sep.
    let %t398 = (let %t399 = Len(arr)
    ICmpEq(%t399, 0))
    case %t398 of
      true →
        ""
      _ →
        letrec join_go = (λi, result.
          let %t400 = (let %t401 = Len(arr)
          ICmpGe(i, %t401))
          case %t400 of
            true →
              result
            _ →
              let %t402 = Index(arr, i)
              case %t402 of
                Some(v) →
                  let %t403 = IAdd(i, 1)
                  let %t404 = (let %t405 = Add(result, sep)
                  Add(%t405, v))
                  join_go(%t403, %t404)
                _ →
                  let %t406 = IAdd(i, 1)
                  join_go(%t406, result))
        let %t407 = Index(arr, 0)
        case %t407 of
          Some(first_val) →
            join_go(1, first_val)
          _ →
            ""

letrec string_len =
λs.
    string_length(s)

letrec str_contains =
λs, needle.
    let slen = Len(s)
    let nlen = Len(needle)
    let %t408 = ICmpGt(nlen, slen)
    case %t408 of
      true →
        false
      _ →
        letrec sc_go = (λi.
          let %t409 = (let %t410 = IAdd(i, nlen)
          ICmpGt(%t410, slen))
          case %t409 of
            true →
              false
            _ →
              let %t411 = (let %t412 = (let %t413 = IAdd(i, nlen)
              Substring(s, i, %t413))
              Eq(%t412, needle))
              case %t411 of
                true →
                  true
                _ →
                  let %t414 = IAdd(i, 1)
                  sc_go(%t414))
        sc_go(0)

letrec total_size =
λa, b.
    let %t415 = __tc_Sizeable_Int_size(a)
    let %t416 = __tc_Sizeable_String_size(b)
    IAdd(%t415, %t416)

letrec size_twice =
λx.
    let %t417 = __tc_Sizeable_String_size(x)
    let %t418 = __tc_Sizeable_String_size(x)
    IAdd(%t417, %t418)

def main =
let %t419 = (let %t420 = total_size(42, "hello")
  Print(%t420))
  let %t421 = size_twice("world")
  Print(%t421)

[1 of 8] Compiling  Assert
[2 of 8] Compiling  Array
[3 of 8] Compiling  IO
[4 of 8] Compiling  List
[5 of 8] Compiling  Numeric
[6 of 8] Compiling  Option
[7 of 8] Compiling  String
[8 of 8] Compiling  class_borrow_calls
