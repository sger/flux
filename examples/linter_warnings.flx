import Math

let unused_top = 1;

module Main {
  fun main(unused_param) {
    let x = 1;
    let y = 2;
    print(Math.square(x));
    let _ = y;
    let f = fun() {
      let x = 2; // shadowed
      return x;
    };
    print(f());
  }
}
