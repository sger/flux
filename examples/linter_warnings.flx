import Math

    fun main(unused_param) {
        let x = 1;
        let y = 2;
        print(Math.square(x));
        let _ = y;
        let f = fun() {
            let x = 2; // shadowed
            return x;
        };
        print(f());
    }
