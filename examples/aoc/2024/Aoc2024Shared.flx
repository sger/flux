module Aoc2024Shared {
    // Safe indexing helper:
    // indexing returns Some(value) or None; default to 0 for AoC numeric paths.
    public fn int_at(arr, idx) {
        match arr[idx] {
            Some(v) -> v,
            _ -> 0,
        }
    }
}
