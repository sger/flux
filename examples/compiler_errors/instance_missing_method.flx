// expect: compile_error
// expect-error: E442
// KNOWN GAP: this fixture declares `expect: compile_error`, but E440/E441/E442
// are still routed as warnings, so the compiler accepts this file and exits 0.
// See the partition in src/compiler/mod.rs (collect_class_declarations).

// Instance that doesn't implement all required class methods.
//
// Expected: E442 Missing Instance Method
//   "Missing method `value` in instance `Describable`."
//   Hint: "`Describable` requires: fn value(...)"

class Describable<a> {
    fn name(x: a) -> Int
    fn value(x: a) -> Int
}

instance Describable<Int> {
    fn name(x) { x }
}

fn main() {
    42
}
