// parity-expected-stdout-begin
// parity-expected-stdout-end
// expect: compile_error

// E400: direct Time builtin call inside function with incompatible ambient effects.

fn bad_clock() -> Int with IO {
    now_ms()
}

fn main() -> Unit with IO {
    bad_clock();
}
