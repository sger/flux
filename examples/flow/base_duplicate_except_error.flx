// parity-expected-stdout-begin
// parity-expected-stdout-end

// expect: compile_error
// Expected compile error E079: duplicate Flow exclusion

import Flow except [print, print]

fn main() with IO {
    Flow.print("unreachable")
}