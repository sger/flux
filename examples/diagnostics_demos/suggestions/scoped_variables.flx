// Test Case 5: Suggestions across scopes

let globalCount = 100
let globalValue = 200

fn test() with IO {
    let localCount = 10
    let localValue = 20

    print(localCount)
    print(localValue)
    print(globalCount)
    print(globalValue)
}

fn main() with IO {
    test()
}
